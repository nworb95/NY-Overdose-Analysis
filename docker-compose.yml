version: '3'

services:
  database:
    image: postgres:latest
    container_name: postgres_db
    volumes:
      - /var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db-data:
    driver: local
